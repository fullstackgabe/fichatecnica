name: Supabase Keep-Alive

on:
  schedule:
    - cron: '30 7 */2 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase (mantem o projeto ativo, evita o pause do free tier)
        env:
          SUPABASE_URL: https://zexoczonpvvxortxniip.supabase.co
          SUPABASE_KEY: sb_publishable_QhY-bXxAoAmb8j_9JAPfXQ_Lreu9q9x
        run: |
          code=$(curl -s -o /dev/null -w "%{http_code}" \
            "$SUPABASE_URL/rest/v1/fichas?select=id&limit=1" \
            -H "apikey: $SUPABASE_KEY" \
            -H "Authorization: Bearer $SUPABASE_KEY")
          echo "Supabase respondeu HTTP $code"
          if [ "$code" != "200" ]; then
            echo "Ping falhou (esperado 200)"
            exit 1
          fi
